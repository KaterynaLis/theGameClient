import React, { Component } from "react";

class Room extends Component {
  render() {
    return (
      <div>
        <button>join this room</button>
      </div>
    );
  }
}

export default Room;
